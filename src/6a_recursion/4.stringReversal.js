import { equal } from "assert";

/*
    Write a recursive function, which for a given string will
    return the reverse of that string

    e.g. reverse('a') = 'a'
    reverse('hi') = 'ih'
    reverse('hello') = 'olleh'
*/
function reverse(string) {}

console.log(reverse("a"));
assert(reverse("a"), "a");

console.log(reverse("hi"));
assert(reverse("hi"), "ih");

console.log(reverse("hello"));
assert(reverse("hello"), "olleh");
